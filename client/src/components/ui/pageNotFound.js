import React from 'react';

export const PageNotFound = () => {

    return (
        <div style={{color: "Red", justify: "center", fontSize: "3rem"}}>
            Search results are not found !
        </div>
    );
};