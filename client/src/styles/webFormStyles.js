import {makeStyles} from "@material-ui/core/styles";

const useWebFormStyles = makeStyles((theme) => ({
    typographyRootHeader: {
        fontWeight: '5px'
    }
}));

export default useWebFormStyles;
